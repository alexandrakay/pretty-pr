# Example workflow — copy this into your repo at .github/workflows/pretty-pr.yml
#
# Requires: ANTHROPIC_API_KEY set in your repo's Settings → Secrets → Actions
#
# name: PRetty
# on:
#   pull_request:
#     types: [opened, edited]
# jobs:
#   generate:
#     runs-on: ubuntu-latest
#     permissions:
#       pull-requests: write
#     steps:
#       - uses: actions/checkout@v4
#         with:
#           fetch-depth: 0
#       - uses: alexandrakay/pretty-pr@v1
#         with:
#           anthropic-api-key: ${{ secrets.ANTHROPIC_API_KEY }}
#           post-if-empty: 'true'   # only post when PR description is blank
#           mode: diff              # commits | diff | full
